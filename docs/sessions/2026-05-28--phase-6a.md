# Session — Phase 6a: Provenance & Audit component (2026-05-28)

**Outcome:** `book-scaffold-astro` **v4.8.0 shipped to npm** — a per-chapter
"How this was made" Provenance/Audit component, auto-rendered on every chapter
of every consumer. Verified on a real consumer (ssm-foundations). The Phase 4
sequencing gate is now **unblocked**.

## Decisions (3 exploration rounds via `/exploring-options`)

| # | Decision | Rationale |
|---|---|---|
| Version | **v4.8.0**, not the spec's "v5" | Additive optional field + new component = MINOR per the repo's own semver (v4.6/v4.7 precedent). Also keeps consumers' `^4.6.0` ranges auto-upgrading. |
| Scope | Full: build → verify → release → real-consumer backfill | Spans 3 repos; satisfies the roadmap "verifies on one consumer" gate against a real book. |
| Fallback | **Global opt-out** | Honor spec [H5] — always render; absent `provenance` → "Audit history not yet recorded". Forces awareness. |
| Insertion | **Auto-inject at the chapter route** | The only mechanism covering all 5 profiles + opt-out. Editing `Chapter.astro` alone would miss tools/minimal/course-notes (3 of 5 route through `Base.astro`). |
| Data scope | New `provenance` only | `ChapterHeader` keeps owning the freshness badge — clean separation. |
| Schema | Nested object; `audit_history.type` free string, `citation_backstop` enum; paths plain `z.string()` (not `.url()`) | Repo-relative paths must parse; controlled vocab only where it drives a CSS class. |

## Spec corrections (the locked plan was stale)

- Monorepo: real paths under `package/`, not `src/`.
- Published version was **v4.7.0** (spec said 4.6.0).
- **Layout-coverage trap**: route-layer insert (not layout) required to cover all profiles.
- Publish is **OIDC on tag push** — not 2FA-blocked (that caveat is manual-publish only).

## PR #81 audit (3 reviewers) → fixes applied before merge

Met the hard standards (Zod DTS single-import, lock-step versioning, SemVer). Gaps fixed:
- Repo's "add a component" checklist: added to `package/CLAUDE.md` + `recipes/04`.
- Component render had no test → `provenance-component.test.mjs` (source-contract). Suite 234→**275**.
- Type hardening (serves the no-silent-failures principle): `.strict()` (reject typo'd keys) + `.refine()` (present ⇒ non-empty).
- Corrected CHANGELOG visual-coverage claim + README component count (39→53).

## Verification

- Package build + 275 tests green; demo (academic/`Chapter.astro`) + course-notes (`Base.astro`) both render the block; fallback on no-`provenance` chapters.
- CI green on PR #81 (test-and-build, smoke-demo, visual). Merged (squash `b3f0dd3`), tagged `v4.8.0` → OIDC published. npm: both packages at 4.8.0.
- **ssm-foundations** (real consumer): ch01 (implemented) renders the populated trail from real `audits/*` files (research-kb backstop), **no dead links**; ch07 (planned) renders the fallback. Committed `966b619` — **not pushed** (push auto-deploys; ssm `main` also carries 2 pre-existing unpushed non-mine commits).

## Follow-ups (tracked)

- Visual-regression baseline for the block (fixtures shadow the injected route → need a fixture through it + CI-env baseline regen).
- Provenance backfill rollout to remaining consumers (DML, dlai-study-notes, guides, claude-books) on their v4.8.0 bumps.
- Held pushes awaiting Brandon: ssm deploy; brandon-behring.dev docs.
