# Session 2026-06-01 — Track 1.5: Credibility, Funnel & Publications

**Trigger.** A fresh-context contrarian audit (`brandon_behring_dev_audit_2026-06-01`,
internally dated 2026-05-31, the day Track 1 shipped) was weighed against the roadmap.
Its load-bearing claims were verified against the live site today — the audit holds up,
and its highest-leverage fixes were **absent from the roadmap**, whose "Next" items were
all *build-more* tasks. Planning ran three `/exploring-options` rounds + a read-only
de-risking pass. Plan: `~/.claude/plans/i-want-to-examine-adaptive-wilkinson.md`.

## Decisions locked
1. **Reprioritize** — cheap credibility/funnel fixes (Track 1.5) before Track 2 content work.
2. **Graph** — fix the data now, **defer densification**; file a research-kb root-cause issue.
3. **Credibility links** — repoint to a public **ADR decision log**; launch a portfolio-wide decision-log initiative (issues + scaffold).
4. **Bucket** — `published-research` → **"Books & Guides"** (name + slug). The old phrase wrongly implied peer-reviewed *journal* papers, which Brandon authors and does **not** conflate with books/guides.
5. **Foreground live** — hero "Live now" strip + move `ssm-foundations` into Books & Guides (structural de-inversion).
6. **Graph titles** — hybrid: **arXiv-accurate title** (matches the linked ID) + keep the curated `key_finding`.
7. **Publications** — add a prominent peer-reviewed homepage section (Brandon supplies the list). **Staged to commit 1.5b.**
8. **Nav** — promote `how-this-was-made` to the top nav.

## Root-cause finding (de-risking)
The lab graph's junk titles trace to **research-kb storing `title = arxiv_id`, `authors = []`,
`year = <arxiv-prefix>`** (`metadata_source: "filename"` — RL papers were auto-ingested by
filename). So research-kb **cannot** supply real titles, and densifying *from* it would import
more junk. Enrichment therefore pulls from the **arXiv API** (offline fallback:
`~/Claude/rl_and_control/references/paper_index.md`). Graph densification is now **blocked-on**
the research-kb metadata-extraction fix.

## Implemented in the working tree — Commit 1.5a (commit + external actions pending)
- **Dead links killed (4):** `how-this-was-made.astro` — `causal_inference_mastery` docs `main`→`master`; the two dead private `DECISIONS.md` examples → the public `prompt-injection-detection-prototype/decisions` ADR log (copy generalized: "a decision log — a `DECISIONS.md`, or a numbered `decisions/` ADR set"). `projects.json` — removed the dead `post_transformers` URL from the ssm entry. All replacements curl-verified 2xx.
- **Bucket rename + move:** `clusters.json` slug/name/description → Books & Guides; `projects.json` 5 cluster refs updated; `ssm-foundations` moved `future`→`books-and-guides` at `order: 1`, existing five renumbered 2–6. Routing is fully dynamic (`work/[slug].astro`); `/work/published-research/` now 404s, `/work/books-and-guides/` builds.
- **"Live now" strip:** new hero section linking the 4 genuinely-live surfaces (ssm-foundations, dml, the lab graph, the OOD study); removed the duplicate hardcoded lab line from the future section.
- **Nav:** added "How this was made".
- **Graph data repair:** `scripts/enrich-citation-graph.mjs` (arXiv API, dependency-free; hand-curated overrides) rewrote `rl_citation_graph.json` — 83 via arXiv + 8 manual, 0 unresolved. Titles/years/authors now real and accurate to the linked arXiv ID (e.g. 1312.5602 → "Playing Atari with Deep Reinforcement Learning", 2013); `key_finding` preserved. `CitationGraph.astro`: panel heading prefers `title`; search filter now also matches `key_finding` + `authors`.
- **Citation accuracy (surfaced by the independent review):** `paper_index.md` mis-assigned arXiv `1406.2199` (actually the "Two-Stream Convolutional Networks" video paper) to DPG. Corrected the node to the real *Deterministic Policy Gradient Algorithms* (Silver et al., ICML 2014; no arXiv id → links the PMLR proceedings); 3 textbook nodes (Sutton & Barto 2nd ed., 2× Bertsekas) had their authors/years completed. The wrong source id is also flagged in the rl_and_control generator issue.

## Completed — outward-facing (post-commit 1.5a)
- **Committed** as `9594ec1` on branch `track-1.5-credibility` (passed the independent-review gate + gitleaks). **Not yet pushed/merged** — deploy is gated on a push to `main`.
- **Tracked issues filed:** `book-scaffold-astro#90` (scaffold decision-log convention), `research-kb#20` (filename-ingest metadata bug — blocks densification), `rl_and_control#1` (generator placeholder metadata + the 1406.2199 mis-map), `causal_inference_mastery#12` + `eval-toolkit#88` (decision-log backfills). Bodies carry label suggestions; the `tracked` label doesn't exist on these repos yet, so board membership can be added manually.
- **Profile README** created + pushed: `github.com/brandon-behring` (README on `main`, verified HTTP 200). Profile **bio/blog** remains a user-run step (`gh` token lacks `user` scope).

## Verification
- `npm run build` clean (13 pages; `/work/books-and-guides/` built, `published-research` gone).
- Static grep of `dist/`: Live-now strip + 4 links present; nav has the page; new ADR/`master` links present, all 3 dead links absent; no `published-research`/`post_transformers` leakage; graph embeds real titles.
- Playwright on `/lab/research-graph/`: 0 console errors; 95 nodes render; detail-panel heading resolves to the real title; search matches `DQN`→3, `atari`→6, `mnih`→2, `mamba`→1 (were 0 before).
- `jq`: 0 junk years, 0 arXiv-id titles.

## Staged — Commit 1.5b (blocked on Brandon's publication list)
Add `src/data/publications.json` + a "Peer-reviewed publications" homepage section (after "What I'm building now"), each DOI/URL verified.

## Track 1.5b — Publications page (this commit)

Brandon's peer-reviewed record was researched + verified this session (authoritative
source: Google Scholar `M1LzNAgAAAAJ`, NYU). Built a **dedicated `/publications/` page**
(`src/pages/publications.astro` + `src/data/publications.json`, 6 entries: 5 journal
articles + the NJIT PhD dissertation). Reverse-chronological; authors abbreviated to 3 +
"et al." when >6 (Behring always bolded, spliced in when past the 3rd position — e.g. the
7-author neuroscience paper); DOI + arXiv links; "Full list on Google Scholar →" foot-link;
no citation counts. Linked from the homepage **About** blurb + the **global footer**; no nav
entry. A dedicated page (not a homepage section) keeps the homepage current-focused — the
intent is a credential, not to be pigeonholed by prior-career work.

Verification catches (pre-build): the *Chaos* DOI is `10.1063/5.0041993` (the alternate
`5.0047025` is a 404); ORCID `0000-0001-6682-7359` is *Carsten* Behring (namesake) → **no
ORCID linked**; all 5 DOIs Crossref-confirmed with matching titles (3 publisher landing pages
return 403 to bots but resolve for humans). One paper Brandon didn't immediately recall —
Goodman & Behring (2023), *Transition to instability of the leapfrogging vortex quartet* —
confirmed his (2nd author; published write-up of his dissertation result; on his Scholar
profile) and **included per his decision**. Build: 14 pages, `/publications/` generated;
Playwright 0 console errors. Deploy via merge to `main`.

## Process insight
Track 1's ambient review checked *claims vs `projects.json`* but missed this audit's class:
no outbound link-integrity sweep, no aggregate-IA judgment, no rendered-data inspection, no
cross-property funnel/credential check. The `independent-review` checklist should grow these —
`verify-rendered-reality-not-notes`, pointed outward.
