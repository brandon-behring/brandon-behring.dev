# Independent review — A4 *1a* overlay consolidation (2026-06-01)

**Driver:** Pre-commit review of the A4 *1a* arc — fold the deleted
`enrich-citation-graph.mjs`'s curation overrides into `rl_and_control`'s exporter,
delete the script, regenerate the site's graph data — across two repos, before
committing/pushing.

**Method:** 2 cold-read `independent-reviewer` shards, fanned out in parallel, each
given only the artifact + a neutral checklist (intended conclusion withheld; `gh`/
network unavailable in-env, so reviewers verified from files + git only). Orchestrator
surfaced verdicts + findings only.

## Findings

| # | Shard | Verdict | Finding | Severity |
|---|-------|---------|---------|----------|
| A | enrich.mjs → exporter port fidelity | PASS (6/6) | All 8 overrides migrated identically; apply-loop + `arxiv_id`-clear + `short_label` arities + junk-title exit all faithful; tests non-vacuous | — |
| A-fix | `apply_overrides_and_labels` | fix | Empty-dict override `{}` — JS counts it applied; Python `if override:` treated `{}` as falsy (skipped, flagged unmatched). Unreachable today (no empty overrides) but a latent footgun + missing test | fix |
| A-info | `json.dumps(ensure_ascii=False)` | info | Reviewer flagged as JS-divergent (`\uXXXX`), but `JSON.stringify` does NOT escape non-ASCII; empirical regression diff shows 0 node-content byte changes → no divergence | info |
| A-info | `_norm_ws` `html.unescape` | info | Decodes more entities than the JS 5-entity decoder, but `surname` = last token makes it unreachable | info |
| B | data regression + roadmap accuracy | PASS (6/6) | 95 nodes / 51 edges preserved; DPG title/no-`arxiv_id`/PMLR-`url` correct (site links from `data.arxiv_id`, not node id); 0 junk titles; diff = edge `weight` 1→1.0 + `generated_at` + `overrides_*` only (benign); every roadmap A4 claim locally SUPPORTED | — |
| B-note | commit atomicity | info | Commit the enrich-deletion + roadmap + data together so "the enrich script is gone" is true at that commit | info |

No regression or data corruption. The port is behaviorally faithful; the regenerated
graph is field-equivalent to the prior render (modulo benign weight/metadata serialization).
GitHub issue open/closed state was the only `not_reviewed` gap (no network) — corroborated
by local commits `41ddec0`/`a273c2e` for research-kb#16/#20.

## Edits applied (review-driven)
- **A-fix**: `apply_overrides_and_labels` now uses `if override is not None:` so an
  empty-dict override counts as applied and is not flagged stale (matches the JS
  truthiness of `{}`); added `test_empty_dict_override_counts_as_matched`.
- **A-info (tests)**: softened the brittle `len(ov) == 8` assertion to `>= 8` + a
  `_comment`-excluded check.
- Re-validated: 29 tests pass; ruff + black clean; fidelity pre-check still 0 node diffs.

## Edits NOT applied
- **A-info (`ensure_ascii`)**: the reviewer's premise (JS escapes non-ASCII) is incorrect —
  `JSON.stringify` preserves literal Unicode, matching `ensure_ascii=False`. The raw
  regression diff confirms no node-content byte change; left as-is (correct for fidelity).
- **A-info (`_norm_ws`)**: unreachable via surname extraction — left as the more-correct
  `html.unescape`.

## Open follow-ups
- None new. (1b densification remains deferred → `research-kb#21`, corpus-acquisition-gated.)

## Reference
- Plan: `~/.claude/plans/use-the-following-handoff-joyful-wigderson.md`
- Closed by this arc: `rl_and_control#1`, `brandon-behring.dev#3`.
- Shipped separately earlier this session: `insurance-ai-toolkit#11` (commit `478d966`).
